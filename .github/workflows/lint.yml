name: Lint

on: [push]
  
jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - name: install node_modules
        run: npm install; npm install node-sass
      - name: eslint
        run: npm run lint
