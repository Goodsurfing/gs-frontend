# Several deps (notably eslint plugins on React 18 + TS 5/6) declare strict
# peer ranges that don't match our installed versions. We pin via package-lock
# anyway, so let npm reuse those resolutions instead of erroring out.
legacy-peer-deps=true
