import React, { FC, memo } from "react";

import styles from "./OfferTermsCard.module.scss";

export const OfferTermsCard = memo((props) => {
    const {} = props;
    return (
        <div className={styles.wrapper}>OfferTermsCard</div>
    );
});
