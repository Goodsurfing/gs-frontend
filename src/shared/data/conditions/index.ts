export interface Transfer {
    id: number;
    name: string;
    imagePath: string;
}

export interface House {
    id: number;
    name: string;
    imagePath: string;
}

export interface Food {
    id: number;
    name: string;
    imagePath: string;
}
